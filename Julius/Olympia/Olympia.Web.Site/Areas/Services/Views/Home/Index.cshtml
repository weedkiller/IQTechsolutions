@{
    ViewData["Title"] = "About Olympia";
}

<!-- Service Section Start -->
<section data-background="images/bg/abs-bg7.png">
    <div class="section-title text-center">
        <div>
            <div class="row">
                <div class="col"></div>
                <div class="col-lg-8 col-xl-6">
                    <div class="section-title-block">
                        <h5 class="text-primary-color anim-box-objects line-both-side mrb-15">Image Upload</h5>

                    </div>
                </div>
                <div class="col"></div>
            </div>
        </div>




        <form id="file-upload-form" class="uploader">
            <p><input type="file" accept="image/*" name="image" id="file" onchange="loadFile(event)" style="display: block; float:inline-start;"></p>

            <p><img id="output" width="500" /></p>


            <button class="btn btn-danger" style="margin:10px;" onclick="closeWindow()">Cancel</button>
            <button class="btn btn-secordary-color" style="margin:10px;">Save &Aacute; Close</button>
        </form>

</div>
    </section>

<script>
    //string root = "wwwroot\\Files";

    var loadFile = function (event) {
        var image = document.getElementById('output');
        image.src = URL.createObjectURL(event.target.files[0]);
    };
    var SavingFile = Url.fileURLToPath("wwwroot\\Files")
</script>

